import React from "react";

export const ButtonCustom = ({ texto }) => {
  return <button>{texto}</button>;
};

// export const Button = ({ children }) => {
//   return <button>{children}</button>;
// };
