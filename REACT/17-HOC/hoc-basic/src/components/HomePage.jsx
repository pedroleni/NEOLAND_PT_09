/* eslint-disable react/prop-types */
// Tenemos un componente HomePage que queremos envolver en el HOC
const HomePage = ({ description }) => <h1>Home 🏠 y {description}</h1>;

export default HomePage;
